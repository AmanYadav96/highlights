<%- include('../partials/head', {titleName: "Finance"}) %>
 
    <body data-sidebar="dark" data-layout-mode="dark" data-topbar="light">

    <!-- <body data-layout="horizontal" data-topbar="colored"> -->

        <!-- Begin page -->
        <div id="layout-wrapper">

            <%- include('../partials/page_top') %>

            <!-- ========== Left Sidebar Start ========== -->
            <%- include('../partials/side_menu', {selected: 23}) %>
            <!-- Left Sidebar End -->

            

            <!-- ============================================================== -->
            <!-- Start right Content here -->
            <!-- ============================================================== -->
            <div class="main-content">

                <div class="page-content">
                    <div class="container-fluid">

                        <!-- start page title -->
                        <div class="row">
                          <% if (user.power == 0) { %>
                            <div class="col-12">
                              <div class="alert alert-warning" role="alert">
                                This is a demo account. Any changes you make will not be saved.
                              </div>
                            </div>
                          <% } %>
                            <div class="col-12">
                                <div class="page-title-box d-flex align-items-center justify-content-between">
                                    <h4 class="mb-0">Verification Request Details</h4>
                                </div>
                            </div>
                        </div>
                        <!-- end page title -->
                        <div class="row mb-4">
                            <div class="col-xl-4">
                                <div class="card h-100">
                                    <div class="card-body">
                                        <div class="text-center">
                                            <div class="clearfix"></div>
                                            <div>
                                                <img src="<%= verDetails.profilePicture ? verDetails.profilePicture : 'https://cdn.icon-icons.com/icons2/1378/PNG/512/avatardefault_92824.png' %>" alt="" class="avatar-lg rounded-circle img-thumbnail">
                                            </div>
                                            <h5 class="mt-3 mb-1"><%= verDetails.name %></h5>
                                        </div>

                                        <hr class="my-4">
                                        <div class="text-muted">
                                            <div class="table-responsive mt-4">                               
                                            <div id="alertContainer"></div>
                                                  <input type="text" id="id" class="form-control" value="<%= verDetails.id %>" hidden>
                                                  <input type="text" id="userId" class="form-control" value="<%= verDetails.user_id %>" hidden>
                                                <div>
                                                    <p class="mb-1">Message :</p>
                                                    <input type="text" class="form-control" id="message" value="<%= verDetails.message %>">
                                                </div>
                                                <div class="mt-4">
                                                    <p class="mb-1">Amount :</p>
                                                    <input type="text" class="form-control" id="amount" value="<%= verDetails.amount %>">
                                                  </div>
                                                <div class="mt-4">
                                                    <p class="mb-1">Status :</p>
                                                    <select class="form-control form-select" id="statuss">
                                                            <option value="0" <%= verDetails.status == 0 ? 'selected' : '' %>>Pending</option>
                                                            <option value="1" <%= verDetails.status == 1 ? 'selected' : '' %>>Approved</option>
                                                            <option value="2" <%= verDetails.status == 2 ? 'selected' : '' %>>Rejected</option>
                                                    </select>
                                                </div>
                                                <div class="d-flex flex-wrap gap-3 mt-3">
                                                    <a type="submit" class="btn btn-primary" id="btnUpdate">Update</a>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-xl-4">
                              <div class="card h-100">
                                  <div class="card-body">
                                    <h5 class="mt-3 mb-1">Account Details</h5>
                                      <div class="text-muted">
                                          <div class="table-responsive mt-4">                               
                                              <div>
                                                  <p class="mb-1">Payment Type :</p>
                                                  <% if(verDetails.payment_type == 1) { %>
                                                    <input type="text" class="form-control" id="payment_type" value="PayPal" disabled>
                                                  <% } else if(verDetails.payment_type == 2){ %>
                                                    <input type="text" class="form-control" id="payment_type" value="Bank" disabled>
                                                    <% }%>
                                              </div>
                                              <% if(verDetails.payment_type == 1) { %>
                                              <!-- <div class="mt-4">
                                                <p class="mb-1">Name :</p>
                                                <input type="text" class="form-control" id="payment_type" value="<%= verDetails.acc_title %>" disabled>
                                              </div> -->
                                              <div class="mt-4">
                                                  <p class="mb-1">Email :</p>
                                                  <input type="text" class="form-control" id="payment_type" value="<%= verDetails.paypal_email %>" disabled>
                                                </div>
                                              <div class="mt-4">
                                                  <p class="mb-1">Username :</p>
                                                  <input type="text" class="form-control" id="payment_type" value="<%= verDetails.paypal_uername %>" disabled>
                                              </div>
                                              <% } else if(verDetails.payment_type == 2){ %>
                                                <div class="mt-4">
                                                  <p class="mb-1">Bank Name :</p>
                                                  <input type="text" class="form-control" id="payment_type" value="<%= verDetails.bank_name %>" disabled>
                                                </div>
                                              <div class="mt-4">
                                                  <p class="mb-1">Account Title :</p>
                                                  <input type="text" class="form-control" id="payment_type" value="<%= verDetails.acc_title %>" disabled>
                                              </div>
                                              <div class="mt-4">
                                                <p class="mb-1">IBAN No. :</p>
                                                <input type="text" class="form-control" id="payment_type" value="<%= verDetails.iban_number %>" disabled>
                                              </div>
                                            <div class="mt-4">
                                                <p class="mb-1">Account No. :</p>
                                                <input type="text" class="form-control" id="payment_type" value="<%= verDetails.acc_number %>" disabled>
                                            </div>
                                                <% } %>
                                          </div>
                                      </div>
                                  </div>
                              </div>
                          </div>
                        </div>
                        <!-- end row -->
                        
                    </div> <!-- container-fluid -->
                </div>
            <!-- end main content-->
            <%- include('../partials/footer') %>
        </div>
        <!-- END layout-wrapper -->

        

        <!-- Right Sidebar -->
        <%- include('../partials/right_sidebar') %>
        <!-- /Right-bar -->

        <!-- Right bar overlay-->
        <div class="rightbar-overlay"></div>

        <!-- JAVASCRIPT -->
        <script src="../assets/libs/jquery/jquery.min.js"></script>
        <script src="../assets/libs/bootstrap/js/bootstrap.bundle.min.js"></script>
        <script src="../assets/libs/metismenu/metisMenu.min.js"></script>
        <script src="../assets/libs/simplebar/simplebar.min.js"></script>
        <script src="../assets/libs/node-waves/waves.min.js"></script>
        <script src="../assets/libs/waypoints/lib/jquery.waypoints.min.js"></script>
        <script src="../assets/libs/jquery.counterup/jquery.counterup.min.js"></script>
        <script src="../assets/libs/apexcharts/apexcharts.min.js"></script>
        <script src="../assets/js/search.js"></script>
        <script src="../assets/js/pages/dashboard.init.js"></script>

        <!-- App js -->
        <script src="../assets/js/app.js"></script>   
        <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/popper.js@1.12.9/dist/umd/popper.min.js" integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
        <!-- App js -->
        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        <script src="assets/libs/parsleyjs/parsley.min.js"></script>
        <script src="assets/js/pages/form-validation.init.js"></script>
        <script>
$(document).ready(function() {  
  $('#alertContainer').empty();
  $("#btnUpdate").click(function(){
    var id = $("#id").val();
    var userId = $("#userId").val();
    var message = $("#message").val();
    var amount = $("#amount").val();
    var status = $("#statuss").val();
    console.log(status);
    updateData(id, message, status, userId, amount);
  });
});

async function updateData(id, message, status, userId, amount) {

  $('#alertContainer').empty();

  $('#btnUpdate').addClass("disabled");
  $('#btnUpdate').html('Saving..');

  $.ajax({
    type: "POST",
    url: "updateWithdrawalRequest",
    data:  {
      id: id,
      userId: userId,
      message: message,
      status: status,
      amount: amount
    },
    success: function (res) {
      if (res.status === 'success') {
        $('#btnUpdate').removeClass("disabled")
        $('#btnUpdate').html('Update');
        $('#alertContainer').html(`
          <div class="alert alert-success" role="alert">
            ${res.message}
          </div>
        `);
        setTimeout(function () {
          $('#alertContainer .alert').hide();
        }, 1000); 
      } else {
        $('#alertContainer').html(`
          <div class="alert alert-danger" role="alert">
            ${res.message}
          </div>
        `);
      }
    },
    dataType: 'json',
  });
}

function showImageInNewTab() {
  console.log(imageUrl);
    var imageUrl = document.getElementById('document').value;
    window.open(imageUrl);
}

        </script>
    </body>

</html>